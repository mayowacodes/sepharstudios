{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Baseline permissions for the desktop shell.",
	"windows": ["main"],
	"permissions": [
		"core:default",
		"shell:allow-open"
	]
}
